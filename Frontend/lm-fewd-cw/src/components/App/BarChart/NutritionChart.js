import React, { useState, useEffect } from 'react';
import Chart from 'chart.js/auto';
import { Bar } from 'react-chartjs-2';

const BarChart = ({ data }) => {
//Bar chart,, non-functional. attempts to gain functionality have all failed. appears to be related to the data being inserted.
  return (<>
    <Bar />
    </>
  );
};
export default BarChart;